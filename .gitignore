node_modules/
database/*.db
database/*.db-shm
database/*.db-wal
.env
*.log
.DS_Store
license.json
dist/
dist-app/
out/
logs/
public/uploads/
src/data/slotime.license

# Backups locales del portátil (no se suben a git)
bkp_portatil/
database/_backup_pre_portatil/
database/_backup_pre_delete/

# Datos generados / de entrada (no son código)
database/sim/
info para proyecto infolap slot/

# Certificados TLS locales (incluyen la clave privada de la CA) — se generan
# solos en cada instalación, NUNCA se suben.
database/tls/

# Datos locales de pruebas de carrera (no son código)
carrera test vicos/
